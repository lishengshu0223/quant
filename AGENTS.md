# 项目说明

这是一个量化金融项目，用于分析和预测金融市场的趋势，并且下载金融数据。

## 技术栈

- Python（执行环境为 conda 的 `multifactor` 环境，运行脚本前需先 `conda activate multifactor`）
- Ricequant SDK（RQData、RQAlphaPlus、RQFactor、RQOptimizer、RQPAttr）

## 项目结构

- `download/` - 金融数据下载
- `research/` - 策略研究
- `update/` - 数据更新
- `llm_mining/` - LLM 因子挖掘（核心代码 `llm_mining/local_miner/`；涉及因子挖掘任务时，先读 `.trae/rules/llm_mining_usage.md` 核心速查，细节再看 `llm_mining/README.md`）
- `local_api/` - 本地 API 服务
- `backtest_results_test/` - 回测结果测试

## 文档分工（各 md 各司其职）

- `.trae/rules/document_index.md` — 仅收录米筐（Ricequant）**外部 API 文档索引**，不含项目自建内容
- `.trae/rules/project_docs_index.md` — **项目自建文档索引**（公告分类体系、每日数据更新、LLM 因子挖掘、事件驱动统计口径、增持研究专档、公告信号实验等）；涉及这些主题时先读该索引
- `.trae/rules/llm_api_usage.md` / `.trae/rules/local_api_usage.md` — 接口调用规范（LLM API / 本地金融数据接口）

## 编码规范

- 使用中文注释和文档
- 遵循 Python PEP 8 规范
- 量化策略代码使用 RQAlphaPlus 框架的约定函数（init, handle_bar 等）
- 获取行情、指数、因子、交易状态等金融数据时，优先使用 `local_api` 读取本地数据（详见 `.trae/rules/local_api_usage.md`），仅在本地数据缺失时才调用 rqdatac 在线 API

## 临时文件管理规范（必须严格遵守）

在执行分析、研究、调试、测试类任务时，agent 产生的文件必须明确分为四类，不得混淆。判断核心依据是**文件是否跨对话仍有价值**：

**① 对话内过程文件（用完即删，禁止放入 `tmp\`）**：在同一轮对话中，为实现用户需求而中途自行生成的测试脚本、调试日志、探针数据、中间结果等过程性文件。
- 这类文件生成后仅在被使用期间允许存在：一旦不再需要**立即删除**，最迟在本轮对话结束时必须清理干净，无需用户提醒
- 禁止抱着"留个底、以后可能有用"的心态把它们放进 `tmp\`——`tmp\` 不是过程文件的回收站；判断标准：**过了这一轮对话，这个文件还值得留吗？** 只对本轮有意义的一律删除
- 确因脚本运行路径限制必须临时写在工作区其他位置时，仍须保持 `tmp_` 前缀，且同样用完即删

**② 跨对话临时文件（唯一允许放入 `tmp\` 的一类）**：模型主动向用户移交、**跨对话仍可能对项目有用**的临时文件，如长任务的断点续跑脚本、可复用的调查/探针工具、未完成任务需在后续对话接续的中间产物。
- 存放于 `f:\quant\tmp\`（不存在则创建），文件名一律以 `tmp_` 开头（如 `tmp_bg_learn.py`）
- 放入 `tmp\` 前必须先自问：这个文件**过了这一轮对话还值得留吗？** 若答案是否定的，归为第①类直接删除
- 程序代码不得依赖 `tmp\` 下的文件，用户可随时手动清空 `tmp\` 而不影响项目运行

**③ 分析成果（保留给用户查看，禁止删除）**：需要用户查看的最终图片、表格、报告等。
- 一律存放在 `f:\quant\output\<YYYYMMDD>_<任务名>\` 子目录下（如 `output\20260805_成交量因子分析\`）
- 文件名可读、带业务含义、不带 `tmp_` 前缀（如 `年度收益对比.png`、`信号明细.xlsx`）
- 任何程序代码不得依赖 `output\` 或 `tmp\` 目录下的文件，用户可随时手动清空这两个目录而不影响项目运行

**④ 正式交付（长期保留）**：用户明确要求沉淀的代码、策略、数据。
- 保存为不带 `tmp_` 前缀的正式文件，放入对应正式目录（`research/`、`download/` 等）

**其他硬性要求**：
1. 任务结束的最终回复中，必须分别列出「保留的成果文件」清单（完整路径 + 一句话说明内容）、「`tmp\` 中保留的跨对话临时文件」清单（如有）和「已删除的过程性临时文件」清单。
2. 严禁擅自删除任何既有文件（含 `tmp\` 中遗留文件）；仅当用户明确要求清理 `tmp\` 时才可动手，并按本规范分类：属于第①类的遗留文件一律删除，属于第②类的保留。
3. 若不确定某个产出用户是否需要保留，先询问，禁止擅自删除。
